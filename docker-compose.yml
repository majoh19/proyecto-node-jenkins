#version de sintaxis
version: "3.9"

#definir servicios
services:
  proyectos-backend-micro:
    container_name: proyectos-backend-micro
    image: proyectos-backend-micro:v1
    build:
      context: .
    ports:
      - 4001:4001
    networks:
      - proyectos-app

networks:
  proyectos-app: